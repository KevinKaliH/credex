export enum EnumAppRoutes {
  base = "/",
  bill = "/bill",
  search = "/search",
  unauthorized = "unauthorized",
}

export enum EnumUrlCatalogsPaths {
  confirm = "/",
  searchBill = "/",
  airpakPayment = "/Transaction/Pago",
  airpakQuery = "/Transaction/Consulta",
  airpakValidateSession = "/ValidarSesion",
}
